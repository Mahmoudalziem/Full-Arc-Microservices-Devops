apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: data
  namespace: sonarqube
spec:
  storageClassName: gp2
  volumeMode: Block
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi